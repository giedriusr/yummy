<%= form_for([:extranet, @extranet_menu]) do |f| %>
  <% if @extranet_menu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@extranet_menu.errors.count, "error") %> prohibited this extranet_menu from being saved:</h2>

      <ul>
      <% @extranet_menu.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
