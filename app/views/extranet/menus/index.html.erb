<h1>Listing menus</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @extranet_menus.each do |extranet_menu| %>
  <tr>
    <td><%= extranet_menu.name %></td>
    <td><%= link_to 'Show', extranet_menu_path(extranet_menu) %></td>
    <td><%= link_to 'Edit', edit_extranet_menu_path(extranet_menu) %></td>
    <td><%= link_to 'Destroy', extranet_menu_path(extranet_menu), confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Menu', new_extranet_menu_path %>
