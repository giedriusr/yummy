<h1>Listing extranet_offices</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Address</th>
    <th>City</th>
    <th>Country code</th>
    <th>Longitude</th>
    <th>Latitude</th>
    <th>Phone</th>
    <th>Email</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @extranet_offices.each do |extranet_office| %>
  <tr>
    <td><%= extranet_office.name %></td>
    <td><%= extranet_office.description %></td>
    <td><%= extranet_office.address %></td>
    <td><%= extranet_office.city %></td>
    <td><%= extranet_office.country_code %></td>
    <td><%= extranet_office.longitude %></td>
    <td><%= extranet_office.latitude %></td>
    <td><%= extranet_office.phone %></td>
    <td><%= extranet_office.email %></td>
    <td><%= link_to 'Show', extranet_office_path(extranet_office) %></td>
    <td><%= link_to 'Edit', edit_extranet_office_path(extranet_office) %></td>
    <td><%= link_to 'Destroy', extranet_office_path(extranet_office), confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Office', new_extranet_office_path %>
